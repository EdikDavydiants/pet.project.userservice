spring:

  application:
    name: User Service

  datasource:
    url: jdbc:postgresql://localhost:5432/UserDB
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
